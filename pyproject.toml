[tool.poetry]
name = "ProAcces-TTS"
version = "0.1.0"
authors = ["Leonardo Blanco Serna <your_email@example.com>"]
description = "Text-to-Speech library for ProAccess"
readme = "README.md"

[tool.poetry.dependencies]
numpy = "^1.21.0"
scipy = "^1.7.0"
pyyaml = "^5.4.1"
PyAudio = "^0.2.11"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"